#include <stdio.h>
int main() {printf(" Welcome to WhileLoop");

int i = 0; // 1 variable declation

while (i < 5) { // 2 condition: condition must be false after some step . or it will be infinite loop .
printf("%d\n", i);
i++; // 3 incrementeion
}

// this loop need 3 part .
// in for loop everything ssame ecxept more compact and less lines

// infinite loop can crash computers. if you see quickly stop program.

return 0;}